name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python=3.8
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - codecov
    - pygame
    

